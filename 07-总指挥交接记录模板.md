# 总指挥交接记录模板

本模板属于抽象层，所有字段由旧总指挥自动读取或生成，不是人工表单。真正使用时，在具体项目管理目录生成实例记录；持久化与逻辑 ID 按索引规范执行，不把项目实例数据写回本目录。

```markdown
# 总指挥交接记录

生成时间：AI 自动记录
项目名称：AI 从项目入口读取
项目根目录：AI 自动定位
状态索引位置与交接记录 ID：AI 自动生成
当前进度视图位置与最后核验截点：AI 自动读取
便携交接快照 ID：AI 自动生成并登记索引
旧总指挥任务 ID：AI 从平台读取；不可得时使用逻辑 ID
新总指挥任务 ID：接管时由 AI 从平台读取；不可得时使用逻辑 ID

## 当前目标

目标版本：
状态：active / paused / superseded / cancelled / completed
目标内容：
范围：
排除项：
验收标准：
停止条件：

## 事实源

项目规则入口：
当前状态入口：
AI 状态索引：
Git/GitHub 当前基线：
相关 Issue/PR 范围快照：
最后可靠状态快照：
最后可靠全量快照：
增量游标或时间截点：
适用规则版本或哈希：
上一轮未闭环对象：
本轮派生对象及交棒状态：

## 活跃任务

| 任务 ID | 任务对象 | 状态 | 基线 | 已完成证据 | 阻断 | 恢复条件 |
|---|---|---|---|---|---|---|

## 本地成果

未提交修改：
本地分支：
worktree：
后台进程：
保留原因：

## 人工与外部门禁

待使用者本人确认：
待本轮协作成员：
待其他团队成员：
待领域专家：
待管理员：
待远端 Review/CI：
待补有价值远端动作：
无需新增远端动作及依据：
临时推进但尚未交接：
Assignee 待复核或待调整：
待达到交棒终点：

## 当前授权包

授权来源：
允许对象与动作：
Assignee 修改授权与禁止项：
禁止项：
有效期与失效条件：

## 交接核验

- [ ] 没有未核验的远端写操作或不可逆动作
- [ ] 所有活跃任务都有可恢复断点
- [ ] 当前状态已保存到稳定截点
- [ ] 未提交修改和 worktree 已明确归属
- [ ] 新旧总指挥不会并行调度
- [ ] 当前授权包已核对，未把旧授权自动继承给新目标
- [ ] 范围、动作回执、有价值远端动作和交棒四类差集为空或已进入明确门禁；“无需新增远端动作”均有依据
- [ ] 最后可靠全量快照、增量游标、未闭环旧账和派生对象均可恢复
- [ ] 已记录本轮适用规则版本，未把执行中更新的规则静默追溯应用
- [ ] 当前进度视图已完成健康检查；如发生压缩，移出内容仍有可回查指针且当前结论等价
- [ ] 便携交接快照包含事实截点、禁止项、证据入口和失效条件

## 新总指挥第一次只读动作

1. 读取项目规则和状态入口。
2. 核对当前 Git/GitHub 基线和任务断点。
3. 只刷新截点后变化、未闭环、派生交付物和交接记录中标记为“受影响/待确认”的对象。
4. 若快照或游标不可靠，退回全量推进；否则输出新的增量事实快照，再决定下一波次。
5. 接管成功后按索引规范更新当前总指挥、交接记录和可靠截点；授权有效性按授权规则重新核验。

## 临时 AI 协作返回包

临时 AI 未取得总指挥调度权时不得直接修改中央状态；可以更新便携快照副本。结束时返回：更新后的快照、实际完成、代码/远端变化及回执、验证、未完成项、阻断、下一责任人、快照是否失效，以及建议由总指挥写回的状态增量。

## 不应从交接记录直接推断的内容

- 当前 GitHub 状态；
- 当前模型或 Codex 窗口真实配置；
- 任务是否仍在运行；
- 领域专家或队友是否已经收到消息；
- 任何远端写操作是否已经完成。
- 本轮使用者本人、本轮协作成员和目标是否仍与上一轮相同；必须以当前指令为准。
- 旧授权是否仍有效；新目标或新轮次不得从交接记录静默继承权限。
```
