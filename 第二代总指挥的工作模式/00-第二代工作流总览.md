# 第二代总指挥工作流总览

版本：2026-08-19.5

定位：适配 Codex Desktop 与 GitHub 协作的“自然语言目标 + 薄总指挥 + Goal + 专项任务”工作流。

状态：第四十五轮修订版，已加入远端交付耗时分级、首次 Push 前 CI 等价门禁、CI 等待期只读并行和稳定截点一次性记账，并保留自动上下文压缩/人工换任务分层、高资源授权、作者侧自修闭环、聚合核验和指纹异常回退机制，可开始受控试运行。

## 1. 一句话说明

本工作流仅直接适用于 GitHub。GitLab、Bitbucket、Azure DevOps 等平台虽然也有相近的 Issue、代码审阅和合并请求能力，但术语、权限、事件与回链行为不同；未经单独适配不得直接套用本流程。

使用者本人只需提供 AI 无法可靠读取或不能替人决定的信息，例如本轮身份、自然语言目标、不可访问的私聊交办和最终授权。其余控制字段由总指挥自动处理。协作型批量任务默认分两阶段：先只读扫描并生成一张人话批量执行卡；使用者本人集中确认一次后，再无人值守执行。

连续讨论以母任务为锚点，后续细节作为决策增量保存。“请执行”默认覆盖唯一活动母任务的全部已确认方案，不只执行最后一个问题；会议或规划可以在同一母任务内连续转为 Issue、实现、测试和真实 PR。

规划转交付与未闭环集中接管同时存在时，默认采用“规划转交付主线 + 集中接管兼容性扫描 + 可选后续推进”。两条队列共享必要事实但分别冻结范围、授权和停止条件；同一对象在当前活动母任务内只保留一个规范执行记录，避免重复派发、重复远端动作和无限范围。

具体项目维护轻量的当前进度视图和状态索引：每次读取时低成本检查是否过期或膨胀，只有命中条件才由唯一维护者压缩。当前进度视图同时承担“灾难恢复热快照”，只保存恢复当前现场所需的目标、基线、队列、断点、验证、禁止项、下一动作和冷证据指针，不复制聊天、完整日志或历史流水。临时交给其他 AI 时生成便携交接快照；未正式转移调度权前，临时 AI 只回传状态增量，不覆盖中央状态。

工作流不得依赖 Codex 账号、任务列表或聊天历史才能恢复。账号额度耗尽、程序崩溃、旧任务不可写或新账号完全看不到历史时，使用者本人只需在目标项目中明确任命新的总指挥并启动空白恢复；新总指挥从项目入口和灾难恢复热快照只读重建现场，核对当前 Git/平台事实后再续作。旧远端授权、运行状态和专业结论不得从快照自动继承。

第二代规则采用“固定编号 + 本地规则根目录 + 文件指纹”定位。首次使用、换电脑或移动规则文件夹后，使用者需要按操作手册提供一次新的本地绝对路径；验证通过后由 AI 写入项目状态索引。之后可以只说文档编号或名称，不必反复上传未变化的规则文件。旧路径失效时 AI 必须在执行项目前先请求新路径，不得猜测或在整块磁盘盲搜。新总指挥任务仍须建立本任务独立加载记录，旧任务的“已读”记录不能跨任务继承；正常交接的候选阶段可以完整读取 `总指挥轻量交接启动配置.md` 并核对来源规则指纹，只有异常项才展开对应编号规则，正式切换后的具体动作仍按场景加载适用规则。

每个已发生实质判断的对象保存一条可迁移决策回执，回答“做了什么、为什么、为何没有采用其他动作、结论何时失效”。总指挥根据任务阶段、稳定截点和本地会话健康自动判断继续当前任务还是建议新开只读解释任务；不按固定轮数、自动压缩次数、压缩前上下文峰值或 Token 占比强制切换，也不要求使用者本人判断上下文健康度。平台在同一任务内多次自动压缩是正常续行机制，不等于必须交接；本地会话日志达到 `200 MiB` 时只触发交接评估，达到 `300 MiB` 时在当前原子步骤收口并形成稳定截点后优先交接，详细规则由 `05-模型选择与资源策略.md` 唯一维护。

图片密集的人工核验采用“压缩前视觉隔离 + 轮次关闭后转冷”两层机制。预计会连续上传多图的大轮次，默认由一个独立、只读的视觉观察任务承载当前轮原图；长寿命总指挥只发送文字测试包并接收结构化视觉结果卡，不把一张图或一个小阶段拆成一个任务。观察任务不取得总指挥权、产品代码写权、中央状态写权或远端授权。原图进入总指挥聊天前完成隔离，才能避免它们继续参与该总指挥任务后续的上下文整理；已经上传到当前聊天的图片无法由本地规则追溯移除。轮次关闭后原件转为默认不回读的冷证据，只有回归、冲突、摘要不足、安全/专业/审计或使用者明确要求时才按证据 ID 最小回读。

场景 2A、2B、3A、3B 结束时，在逐编号状态之外自动输出与本场景有关的关系、当前行动方，以及使用者本人的“必须人工操作”和“建议人工操作”。2A、3A、3B 继续输出 PR 堆叠树和 Issue 依赖树；2B 输出本地基线、检查点、继承远端对象和发布门禁。所有视图只投影本轮已核验事实，不替代 Base/Head、逐编号事实或远端授权；等待状态必须写清当前行动方和后续行动方，避免双方互等。

## 2. 工作流结构

```mermaid
flowchart TD
    _01["1. 使用者本人提出自然语言目标"] --> _02["2. 读取团队与仓库规范"]
    _02 --> _03{"3. 判断任务类型"}
    _03 --> _04["4. 只读扫描、必要验证、范围核账与对抗式审查"]
    _04 --> _05["5. 送达一次性批量执行卡"]
    _05 --> _06{"6. 使用者本人是否集中确认？"}
    _06 -->|否| _07["7. 不分配实现任务，安全等待"]
    _06 -->|是| _08["8. 拆分有限 Goal 和专项任务波次"]
    _08 --> _09["9. 授权内本地或远端动作进入可执行队列"]
    _09 --> _10["10. 执行专项任务波次"]
    _10 --> _11["11. 局部实现、测试、远端交付与证据回传"]
    _11 --> _12["12. 更新状态并核验本地检查点、协作价值、动作、回执与交棒终点"]
    _12 --> _13{"13. 是否仍有可安全推进事项？"}
    _13 -->|是| _08
    _13 -->|否| _14["14. 汇报完成、阻断清单与下一步"]
    _02 -.-> _15["15. 接收目标变更或项目事件"]
    _15 --> _12
    _02 -.-> _16["16. 账号断联或空白任务：读取灾难恢复热快照"]
    _16 --> _12
```

## 3. 四层职责

### 使用者本人层

当前向 AI 发出指令并承担最终确认的人。提出目标、范围、验收标准、优先级和限制；默认只需在批量执行卡形成后集中确认一次。若希望真正只发一条消息就离开，可以在对象、动作和禁止项已经明确时随 Goal 提交精确预授权包，但这不是经验不足阶段的推荐默认值。

### 总指挥层

只负责控制面：输入完整性、建议审查、事实核验、范围核账、依赖分析、优先级、可执行队列、资源门禁、状态汇总、动态重规划和流程复盘。总指挥不直接修改产品代码。

### 专项任务层

一个任务只负责一个边界清楚、可独立交付和验证的工作单元；它可以覆盖共享同一根因、同一 PR 或严格依赖链的多个编号。每个编号仍须单独记录状态。实现遵循“正确与安全优先、简单方案优先、抽象和运维能力按证据引入”，任务负责实现、测试、自检和证据回传；结束时先用 1—2 句人话说明效果。需要当前使用者本人参与时给出可执行操作包；其他成员的动作进入协作交接，不混入使用者本人的待办。

### 项目实例层

保存具体项目的代码、规则、Issue、PR、成员、路径、当前目标、任务状态、Base/Head、测试结果、交接记录和 `AI状态索引.md`。第二代抽象层只保存索引规范，不得写入这些实例数据。

## 4. 事实优先级

当前代码和测试结果 > 当前 Git/GitHub 状态 > 项目规则和状态入口 > 已确认的会议记录 > 旧聊天记录和旧 AI 结论。

无法确认的内容统一标记为“待确认”，不能因为旧文档写过就当作当前事实。

## 4.1 永久规则优先级

系统和平台限制 > 当前团队及目标仓库规则 > 分支保护、CODEOWNERS 和 CI 门禁 > 使用者本人本轮授权包 > Goal 与专项任务卡。批量授权不能覆盖更高层规则；团队文档禁止的动作即使被使用者本人误点同意也不得执行。在硬安全边界内，仓库具体流程优先于通用工作流偏好；缺 Reviewer 等低风险流程缺口只暂停受影响阶段，不使全量推进整体停止。

本工作流禁止 Force Push、直接写受保护分支、绕过 Review/CI/分支保护、使用未知凭据和秘密、删除远端历史。即使使用者本人误点同意也不得执行；确有灾难恢复等例外需求时，必须退出本工作流，改用团队正式事故流程和专门审计。

## 5. 七类执行模式

1. 普通目标：处理一个有限目标，默认使用一个 Goal 和一到多个专项任务波次。
2. 规划转交付：把会议、问题或路线规划去重并转为 Issue；端到端模式在一次集中确认后继续完成可执行项的实现、测试、真实 PR 和 Reviewer 请求，仅建档模式则停在 Issue 闭环。
3. 本地闭环实现与集中发布准备：自动判断新目标、已知本地续作、本人远端对象续作、他人对象推进、混合或恢复场景；把会议总结、规范、附件和反例作为分级证据输入，由 AI 生成需求—证据—验收矩阵和人工核验插入点。面对现有 PR 时，AI 分别判断是否需要远端 Review 门禁、由谁实现、在哪个分支实现，以及当前处理者在修复后是否仍是仓库认可的有效 Reviewer，并用人话解释选择；能够在不扰乱团队开发的前提下直接修复并有效 Approve 时，不为制造留痕先增加低价值 Comment、Request Changes 或交接等待。达到发布候选后，先把本地 Commit、文件或代码块按现有 Issue/PR 的需求、责任和 PR Head 建立发布归属矩阵；一个本地提交跨多个责任对象时允许在锁定的目标 Head 上拆分移植，原存档分支保持不变。随后单独生成远端发布卡。正常路径要求完成规定的人工验收；时间紧急时，只有使用者本人明确说明豁免原因、未完成项、风险接受和精确远端终态动作，才能进入紧急发布豁免审查。豁免不得写成验收通过，也不能覆盖 Required Checks、有效 Review、权限、安全或平台保护。本地 Commit 不等于 Push；AI 与使用者本人验收不能替代仓库明确要求的独立 Review 或专业验收，但参与修复也不自动产生一轮额外复审。
4. 协作队列全量接管推进：在目标模块边界内重建完整协作候选快照，不以 Issue/PR 创建者作为唯一入口，也不因协作成员被列名就接管其全部任务。默认覆盖已列协作成员创建且内容命中目标的对象、使用者本人创建且内容命中目标并出现新协作活动的对象、目标外强参与信号、明确交办、未闭环旧账、派生对象和最小必要依赖；随后区分执行对象、上下文依赖和排除对象，持续推进所有可执行项，并建立后续增量推进所需的可靠截点。扫描入口中发现的跨模块弱关联对象单独汇总，默认排除，不因整卡确认而自动执行。
5. 协作队列增量推进与覆盖核账：自动读取最近可靠截点，处理新增/变化对象、新协作事件、未闭环旧账和派生交付物；基准不可靠时自动转为全量推进。具体候选算法由核心规则维护，检索与回退由索引规范维护。
6. 控制面工作：只汇报进展、拓扑、状态变化、目标变更、交接或复盘，不启动产品实现。
7. 空白账号灾难恢复：在旧任务不可读取或不可继续时，由使用者本人明确任命新总指挥；新总指挥读取项目入口、当前进度视图和状态索引，验证控制面世代、工作区、Git/平台事实和恢复断点。恢复阶段只读，不继承旧远端授权，不重新执行已经有完成证据的批次；事实可靠后写入新的接管世代和恢复截点，再继续原队列。

全量和增量推进都采用“一编号一状态、一独立交付单元一责任窗口”，不机械地为每个编号单开窗口。增量推进不是“只扫新编号”：它必须同时核对新增/变化对象、既有对象上的新 Comment/Review/Commit/Push/Head/Base 或状态事件、未闭环旧账和派生交付物；可靠截点丢失、目标边界改变或事实冲突时自动退回全量推进。单项阻断只进入阻断队列，总指挥必须重新计算其他可执行项；堆叠或 Draft PR 先过低成本可审查性门禁，不能仅因父项未合入就统一暂停。

远端身份以 GitHub 登录账号为主键，不要求填写平台名、真实姓名或称呼。目标和协作成员都支持单值或多值；多值只形成待匹配集合，不做两者的笛卡尔积，也不自动扩大责任或授权。协作成员被列名本身不构成接管其全部任务的理由；每个正式候选都必须有对象内容命中目标、目标外强参与信号、未闭环/派生关系或最小必要依赖之一作为证据。使用者本人创建对象上的新协作活动若不命中目标，也必须满足目标外强信号门禁。跨模块只有弱关联时，AI 仅在批量执行卡的独立观察区说明对象、依据和潜在价值；使用者本人未明确选择时保持排除。

协作可见性按层级记录：未处理、仅本地、汇总载体远端可见、对象级远端可见、本地与对象级远端均处理、远端执行待确认。只有目标对象时间线上出现本轮直接动作，或出现本轮新生成且可核验的 Cross-reference/平台回链，才算对象级远端可见；旧回链或只在父项正文中出现编号不算本轮新留痕。协作可见性与远端动作必要性分开记录：现有记录已经完整表达事实、责任与恢复条件时，可以有依据地判定“无需新增远端动作”，这不是留痕遗漏。长任务只在关键检查点做一次性、低频的内存查询。

Issue 的贡献事实与后续责任分开管理。提交关联 PR、修复或验收证据后必须复核责任归属，但不自动修改 Assignee：正式负责、临时推进和一次性交付使用不同协作动作。只有责任依据、目标账号的持续负责意愿或明确安排、团队规则、平台权限和本轮精确授权同时成立时，才可设置对应 Assignee；不得自动替换或移除其他成员。

## 6. 第二代与第一代的关系

第二代是独立控制面，不修改、不覆盖、不共享第一代的提示词、事件账本、任务 ID、自动化、状态文件或交接记录。第一代仅作为历史经验和只读参考，详见 `08-第一代隔离与迁移规范.md`。从未使用第一代的新项目无需准备任何第一代文件，相关位置保持“未配置”即可。

## 7. 推荐使用顺序

- 人类只需先看 `01-操作者操作手册.md`。
- 总指挥首次启动时读取 `02-总指挥核心规则.md`。
- 需要创建专项任务时按 `03-专项任务卡模板.md`。
- 动态变更和交接按 `04-状态、目标变更与交接规范.md`、`07-总指挥交接记录模板.md`；正常交接候选的第一次只读按 `总指挥轻量交接启动配置.md`。
- 模型配置按 `05-模型选择与资源策略.md`，复盘按 `06-复盘与优化规则.md`。
- 授权和风险分级按 `09-自动化授权与风险分级.md`。自动 ID、快照、游标和历史检索按 `10-自动状态索引规范.md`。

完整编号注册表如下。表中只保存可分发的相对文件名；具体电脑上的绝对路径只能写入项目实例的 `AI状态索引.md`。

| 编号 | 相对文件名 | 主要用途 |
|---|---|---|
| 00 | `00-第二代工作流总览.md` | 总览、导航与编号注册表 |
| 01 | `01-操作者操作手册.md` | 使用者入口与可复制场景提示词 |
| 02 | `02-总指挥核心规则.md` | 总指挥控制面内核 |
| 03 | `03-专项任务卡模板.md` | 专项任务契约与回传格式 |
| 04 | `04-状态、目标变更与交接规范.md` | 状态、变化、证据摘要与交接 |
| 05 | `05-模型选择与资源策略.md` | 模型、Token 与资源选择 |
| 06 | `06-复盘与优化规则.md` | 复盘、规则修改闭环与文档治理 |
| 07 | `07-总指挥交接记录模板.md` | 交接记录结构 |
| 08 | `08-第一代隔离与迁移规范.md` | 第一代隔离边界 |
| 09 | `09-自动化授权与风险分级.md` | 风险、授权与远端写入门禁 |
| 10 | `10-自动状态索引规范.md` | 本地索引、规则入口、指纹与截点 |

非编号启动配置：`总指挥轻量交接启动配置.md`。它只服务场景 6A 的候选只读和最终切换前复核，不扩展 00～10 编号注册表，也不代替后续动作所需的正式编号规则。
