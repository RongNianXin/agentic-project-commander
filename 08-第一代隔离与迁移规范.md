# 第一代隔离与迁移规范

## 1. 目录隔离

- 第一代目录：总指挥从已知工作流父目录、历史配置和项目入口只读搜索后自动定位。
- 第二代目录：以本规则文件所在目录自动定位。
- Goal 经验记录目录：从同级目录、项目入口和已有状态索引自动定位。

首次定位成功后，将规范化路径和核验依据写入具体项目的 `AI状态索引.md`，后续自动复用并检查路径是否仍有效。不存在候选时标记“未配置”；存在多个不可判优候选时只请求使用者本人确认一次路径。不得要求日常任务重复填写目录，也不得为了自动化猜选并写入不确定目录。

三者互为只读参考，不把它们混合成一个当前事实源。

## 2. 禁止共享的内容

第二代不复用第一代的：

- 总指挥任务 ID；
- 专项任务 ID；
- 事件账本、队列和自动化；
- 项目状态面板；
- 交接记录；
- 版本号、同步脚本生成块或受管规则块。

可以复用的是经过审查后的经验、术语、安全边界和设计原则；复用时应写入第二代自己的版本文档。

## 3. 具体项目隔离

第二代抽象层不迁移、不覆盖具体项目管理目录。项目细节继续留在项目自己的管理层中，通过入口、适配器或人工提供的路径按需读取。

如果未来要把某个项目从第一代接入第二代：

1. 先建立只读映射表；
2. 对任务、状态、文件和事件去重；
3. 为目标和工作项生成第二代版本；
4. 影子运行并比较结果；
5. 经使用者本人确认后，才切换控制面；
6. 保留第一代记录为只读历史，不删除、不覆盖、不整体搬迁。

## 4. 旧文件的处理

第一代文件不改名、不移动、不删除。第二代文档中如需引用旧经验，只写来源路径和结论摘要，不复制大段正文。

## 5. 污染判定

出现以下任一情况，暂停第二代调度并报告：

- 读到第一代事件账本却无法确认其是否仍在使用；
- 发现同一项目存在两个总指挥都在派发任务；
- 第二代任务写入第一代状态文件；
- 第一代旧提示词覆盖了第二代当前规则；
- 无法判断某个任务或状态记录属于哪一代。
