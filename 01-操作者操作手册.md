# 第二代工作流操作手册

## 下载后 3 步启动

1. 下载或克隆本仓库到一个稳定的本地目录，确认该目录直接包含 `00～10` 文档。
2. 在 Codex Desktop 中打开真正要开发或管理的目标项目，而不是本规则仓库；只有维护规则本身时才把本仓库作为目标项目。
3. 复制下方“登记规则文档目录”提示词，把占位符替换为本仓库的本地绝对路径；登记通过后发送“场景 1”，再按需要选择其他场景。

如果 Codex 无权读取规则目录，请先把该目录加入当前可读工作区，或只把 `00～10` 复制到目标项目中已被 Git 忽略的本地规则目录，再登记新路径。目标项目没有合适的状态目录时，AI 可能要求你确认一次 `AI状态索引.md` 的存放位置；应选择项目已有的个人、私有或本地管理目录，并确保它不会被提交到团队仓库。

## 使用者须知
1. 本工作流仅直接适用于 GitHub；其他平台要先单独适配。
2. 打开一个总指挥任务，从下表选择场景并复制提示词；不确定时先用场景 1 恢复事实。不要一次性上传全部规则文档。
3. 只填写 AI 无法读取或不能替你决定的内容。轮次、快照、任务 ID、分支和历史截点由 AI 自动处理。
4. `【】` 中是要选择或填写的内容。多个目标或账号用逗号分隔；必填占位符未替换时，AI 必须停止并指出缺项。
5. 开放式字段会在后面提供 `(示例：……)`；示例只帮助理解，不是必须照抄的答案。纯选择题不附示例。
6. 协作开发默认分两步：AI 先只读扫描并生成一张批量执行卡；你集中确认一次后，AI 再无人值守执行卡内动作。
7. 账号只用于识别身份，不自动代表 Assignee、实现者、PR 作者、Reviewer 或远端授权；具体责任必须另行判断。
8. 每项任务结束时，AI 应用 1—2 句人话说明结果和是否需要你操作；需要你参与时，必须给出步骤、目的、预期结果和异常反馈方式。
9. 在 VS Code 中快速浏览小节目录：按 `Ctrl+Shift+O` 打开当前文档的标题列表，输入场景编号或标题即可跳转；需要常驻目录时，选择“查看 → 打开视图 → 大纲”，点击侧边栏标题定位。不要通过破坏标题层级来放大章节标题。
10. 每个编号保留独立状态；“被发现”不等于必须执行或评论。Approve、Merge、Close、部署和不可逆操作仍须遵守单独门禁。
11. 场景 2A、3A、3B 结束时，AI 会自动给出 PR/Issue 关系树、必须人工操作和建议人工操作；无需再额外调用场景 4A。只有想重新汇总、补看或怀疑遗漏时才使用场景 4A。
12. 是否继续当前任务由 AI 根据稳定截点、未完成工作和可迁移回执自动判断并提醒。刚做完某个对象后的少量追问通常留在原任务；主任务正继续推进、追问较多且目标对象已有完整回执时，AI 才建议另开只读解释任务。不要按固定对话轮数或 Token 数自行切换。
13. 规则文档保存在本地时，首次使用、换电脑或移动规则文件夹后，先使用下面的“登记规则文档目录”。以后可以只说“读取 02、03、04、09”，不必反复上传未变化的附件。同一任务内文件未变化时 AI 可复用已读记录；新任务会从已登记路径重新读取适用规则，不要求你重新上传。

快速定位：

| 现在要做什么 | 直接使用 |
| --- | --- |
| 首次使用、换电脑或移动了规则文件夹 | 登记规则文档目录 |
| 首次启动或恢复总指挥 | 场景 1 |
| 提出普通开发目标 | 场景 2 |
| 把会议、问题或规划转为 Issue，并继续实现到 PR | 场景 2A |
| 全量接管当前协作队列中的 Issue/PR | 场景 3A |
| 只检查协作队列的新对象、新回应、代码变化和历史漏项 | 场景 3B |
| 在新任务中精确解释上一轮某个 Issue/PR | 场景 4B |
| 汇总进展、人工操作、目标变更、交接或复盘 | 场景 4～7 |
| 立即暂停 | 场景 8 |

全量推进适合首次接管、范围或协作者变化、长期未处理、怀疑遗漏或历史截点不可靠时，用来重建当前完整协作队列。增量推进适合已有可靠截点后的高频交替协作，只检查新增/变化对象、未闭环旧账、派生对象和最小依赖；它不是只看新编号。任务数量少不等于可以直接增量，新开任务也不等于必须全量，关键看可靠截点能否恢复。拿不准时可以直接使用场景 3B，由 AI 验证截点，不可靠时自动转为全量。

账号、范围门禁、责任判断、远端授权、语言和输出细则由核心规则统一维护；操作手册不重复展开。使用者本人、本轮协作成员或其他 AI 的建议都要先核对事实和风险，不能直接当成成立的结论。

## 首次使用、换电脑或移动目录：登记规则文档目录

只有首次使用、规则文件夹被移动或换电脑时需要执行一次。请复制下面内容，把路径替换成当前存放 `00～10` 文档的文件夹；平时执行场景 2A、3A、3B 等任务时不必重复填写。

```text
第二代规则文档目录：【本地绝对路径】(Windows 示例：C:\path\to\agentic-project-commander；macOS/Linux 示例：/path/to/agentic-project-commander)
请验证该目录包含《00-第二代工作流总览.md》，并按其中编号注册表核对规则文件。验证通过后，把新的规则根目录、注册表版本和逐文件指纹写入当前项目的 AI 状态索引；旧路径标记为失效。只登记规则入口，不修改产品代码，不执行远端写操作。
```

预期结果：AI 明确回复“规则目录验证通过并已登记”，列出识别到的规则版本和缺失文件。若路径不存在、目录不完整或状态索引不可写，AI 必须停止登记并说明具体问题，不能假装成功；你只需提供新路径或修复后的文件夹，不必逐个上传文档。

## 场景 1：首次启动或恢复总指挥

读取文档：`02-总指挥核心规则.md`、`09-自动化授权与风险分级.md`、`10-自动状态索引规范.md`

复制发送：

```text
第二代规则文档目录：【自动定位 / 本地绝对路径】(Windows 示例：C:\path\to\agentic-project-commander；macOS/Linux 示例：/path/to/agentic-project-commander)
首次使用、换电脑或移动过规则文件夹时填写当前绝对路径；其他情况保留“自动定位”。
请按第二代规则编号读取 02、09、10，在当前项目建立或恢复唯一总指挥。优先从项目状态索引登记的本地规则根目录读取；入口缺失时再请我提供一次规则文件夹路径，不要要求我逐个重复上传规则附件。
先只读确认项目范围、规则入口、当前目标、现有任务、代码/Git 状态和远端状态。
判断这是首次启动、正常恢复、目标变更还是控制面冲突。
不要修改产品代码，不执行远端写操作，不创建重复状态记录。
最后只汇报：当前目标、事实基线、活跃任务、阻断、下一波次和需要我确认的事项。
```

## 场景 2：提出一个普通开发目标

读取文档：`02-总指挥核心规则.md`、`03-专项任务卡模板.md`、`09-自动化授权与风险分级.md`

复制发送：

```text
请围绕下面目标规划并执行一个有限 Goal：
目标：【本轮目标】
补充限制或业务验收：【无 / 具体限制或验收要求】(示例：只修改网页端，不调整后端接口)
请自动读取当前项目、规则、状态索引和相关代码，先草拟范围、验收标准、排除项、基线与停止条件。只有不同解释会显著改变范围、风险、成本或对外行为时，才向我提出最少问题。
第一阶段先读取团队和仓库规则，只读扫描最小必要事实并做必要验证；不要修改产品代码，不要分配具体实现任务，不要执行远端写入。
完成范围、依赖、风险和验证计划后，生成一张人话批量执行卡：推荐执行/不执行、最坏后果、硬禁止项、自动执行范围和紧急停止条件；技术精确值放附录。
收到我一次集中确认后，才拆分专项任务并无人值守执行。授权外动作只暂停受影响项，继续其他已授权工作，最终统一汇报。
```

## 场景 2A：把会议或规划转为 Issue，并端到端推进

读取文档：`02-总指挥核心规则.md`、`03-专项任务卡模板.md`、`04-状态、目标变更与交接规范.md`、`09-自动化授权与风险分级.md`

默认使用端到端推进；只想建档、不改代码时，把“推进模式”改为“仅创建 Issue”。下面的可选设置已经填好默认值，没有特殊安排时直接保留；多个值统一用逗号分隔。复制发送：

```text
请把以下会议结论、问题描述或规划转为可执行工作项，并按本轮推进模式处理。
使用者本人平台账号：【平台账号】
目标模块或母任务：【一个或多个目标】(示例：目标模块1，目标模块2)
本轮协作成员平台账号：【一个或多个平台账号 / 无】(示例：账号1，账号2)
推进模式：端到端推进（只想建档时改为“仅创建 Issue”）
历史协作任务处理：只登记、不执行（希望本轮完成后继续处理时，改为“完成本轮后继续处理”）

Issue 负责人账号：【未指定 / 一个或多个平台账号】(示例：账号1，账号2)
Issue 候选负责人账号：【无 / 一个或多个平台账号】(示例：账号1，账号2)
候选负责人只用于提供人选，不得自动设为 Assignee。
本轮实现与 PR 推进责任：【使用者本人优先 / 指定平台账号 / 按对象分别指定 / 由 AI 根据现有责任证据建议】
责任依据或对象例外：【无 / 具体要求】(示例：小组会议决定：目标模块的 Issue 由使用者本人提出并优先实现；协作成员主要负责交叉审阅)
代码 PR 审阅人账号：【自动按仓库规则判断 / 一个或多个平台账号】(示例：账号1，账号2)
代码 PR 审阅人负责检查后续代码改动，不是 Issue 负责人；仅创建 Issue 时不会请求其审阅。
不同目标的人员安排：【无 / 按目标说明人员安排】(示例：目标模块1由账号1实现、账号2审阅；目标模块2由账号2实现、账号1审阅)
填写“无”表示全部目标共用上面的设置。
最低有效批准数：由 AI 按仓库规则自动读取；只有需要提高门槛时才把本行改为明确数字

本轮新增附件或原始记录：【无 / 本地路径或附件名称】(Windows 示例：C:\path\to\meeting-notes.txt；macOS/Linux 示例：/path/to/meeting-notes.txt)
补充业务限制或非平台交办：【无 / 可核验摘要】(示例：会议决定先完成网页端，再处理移动端)

先读取团队和仓库的 Issue 模板、标签规范、贡献规则、分支保护、Review 和合并门禁；扫描现有 Issue/PR 去重并建立依赖拓扑。附件先按必要、辅助、重复分类，已有可靠摘要且原件未变化时不要重复读取。

若存在未闭环的集中接管队列，按核心规则先做低成本兼容性扫描，防止重复对象、错误 Base 和堆叠冲突；该扫描本身不等于授权完整 3A。

第一阶段只读生成一张合并批量执行卡，覆盖：建议创建或更新的 Issue、正文与标签、责任安排、实现单元、任务分支、Push、真实代码或文档 PR、Issue 关联、Reviewer 请求、验证、失败回退、禁止项和交棒终点。Issue 编号尚未生成时使用稳定草案键；创建后自动回填编号和关联，不要求我再次确认。

我集中确认一次后，在同一母任务中连续执行：创建并回读 Issue → 推进当前可执行项 → 修改与测试 → Push 并创建真实 PR → 满足仓库条件时转 Ready 并请求 Reviewer。不得创建没有真实交付的空 PR；Epic、调研、决策或领域取证 Issue 不强制对应 PR。

缺少 Reviewer、辅助附件或其他非当前阶段条件时，只暂停受影响动作并继续其余工作。不得违反仓库规则；推进到规则允许的最远位置后，统一报告待补门禁和通用流程不适配项。Approve、Merge、Close、部署及其他高风险动作仍按授权规则单独处理。结束时按核心规则自动输出 PR 堆叠树、Issue 依赖树、当前行动方，以及使用者本人的必须/建议人工操作。
```



同一活动母任务讨论期间，后续问题视为修正增量。全部细节确认后，可以只发送：

```text
请执行当前母任务的完整已确认方案。合并后续全部修正，以最后一次明确结论为准；未决事项排除，远端写入先生成一张合并批量执行卡。
```

只有存在唯一活动母任务、已确认结论和排除项可以可靠恢复时，这条短指令才生效；否则总指挥先列出冲突或待决项，不得只执行最后一条细节。


## 场景 3A：全量接管协作队列中的 Issue/PR

读取文档：`02-总指挥核心规则.md`、`03-专项任务卡模板.md`、`09-自动化授权与风险分级.md`

复制发送：

```text
请对【一个或多个目标】(示例：目标模块1，目标模块2)当前协作队列中的 Issue 和 PR 执行全量接管推进。
使用者本人平台账号：【平台账号】
本轮协作成员平台账号：【一个或多个平台账号】(示例：账号1，账号2)
范围补充或限制：【无 / 具体补充或排除范围】(示例：排除仅涉及部署的对象)
本轮实现与 PR 推进责任：【使用者本人优先 / 指定平台账号 / 按对象分别指定 / 由 AI 根据现有责任证据建议】
责任依据或对象例外：【无 / 具体要求】(示例：目标模块由使用者本人优先实现；已有负责人或活跃分支的对象保持原安排)
默认纳入任务规则：目标外任务只有在正式请求使用者本人 Review、直接点名、分配给使用者本人、明确交办，或对使用者本人已有参与作出可核验的直接回复时，才进入正式候选；跨模块弱关联只汇总并等待确认。该规则同时适用于已列和未列的团队成员。
私聊/会议交办：【无 / 原文或可核验摘要】(示例：会议决定由使用者本人处理目标模块，其余成员负责审阅)
协作轮次、规则版本、状态快照、启动截点和任务 ID 由总指挥自动处理，不需要我填写。
若必填占位符仍未替换，立即停止并列出待补字段，不要自行推断。

范围不能只按创建者过滤，也不能因某人出现在协作成员身份卡中就接管其全部任务。按核心规则自动覆盖目标内对象、目标外强参与信号、未闭环旧账、派生对象和最小依赖；使用者本人创建对象上的新协作事件若不命中目标，也必须通过目标外强信号门禁。没有明文回复的代码变化必须核验对象、分支和关联关系。每个正式候选必须写明纳入依据；没有依据时排除。跨模块弱关联对象只做低成本登记并在批量执行卡中单列询问；我未明确选择时不得执行。随后完成依赖分类、堆叠关系和可执行顺序，每个执行对象保持独立状态，单项受阻不得暂停其他可执行对象。

第一阶段只读扫描和必要验证，不创建实现任务、不改产品代码、不执行远端写入。生成一张批量执行卡，逐对象说明建议动作、协作价值、风险、验证、禁止项、失败回退和交棒终点。

收到我一次集中确认后，按授权规则无人值守推进。结束时重新覆盖核账，区分本地与远端结果，输出建议查看顺序、逐编号状态、PR 堆叠树、Issue 依赖树、当前行动方、真实缺口、使用者本人的必须/建议人工操作，以及给本轮协作成员的短交接包。
```

## 场景 3B：协作队列增量推进与覆盖核账

读取文档：`02-总指挥核心规则.md`、`03-专项任务卡模板.md`、`04-状态、目标变更与交接规范.md`、`09-自动化授权与风险分级.md`

复制发送：

```text
请对【一个或多个目标】(示例：目标模块1，目标模块2)执行一次协作队列增量推进与覆盖核账。
使用者本人平台账号：【平台账号】
本轮协作成员平台账号：【一个或多个平台账号】(示例：账号1，账号2)
范围补充或限制：【无 / 具体补充或排除范围】(示例：只复查上轮未闭环对象和新 Review)
本轮实现与 PR 推进责任：【使用者本人优先 / 指定平台账号 / 按对象分别指定 / 由 AI 根据现有责任证据建议】
责任依据或对象例外：【无 / 具体要求】(示例：新增可执行项由使用者本人优先实现；已有负责人保持不变)
默认纳入任务规则：目标外任务只有在正式请求使用者本人 Review、直接点名、分配给使用者本人、明确交办，或对使用者本人已有参与作出可核验的直接回复时，才进入正式候选；跨模块弱关联只汇总并等待确认。该规则同时适用于已列和未列的团队成员。
私聊/会议新增交办：【无 / 原文或可核验摘要】(示例：会议新增要求先处理目标模块的接口问题)
自动识别增量基准并用一句话回显依据；基准不可靠时转为全量推进，不要求我查询或填写历史 ID。若我要指定特殊历史轮次，会在本指令末尾主动说明。

按核心规则只读检查新增/变化对象、既有对象上的新协作事件、未闭环旧账、派生交付物和最小依赖。使用者本人创建且出现本轮协作成员新 Comment、Review、Commit/Push、关联交付或状态变化的对象必须经过参与信号判断；只有能证明本轮需要受理或回应时才纳入正式候选。扫描入口中发现的跨模块弱关联对象单列为默认排除的观察项；我未明确点名确认时不得执行。增量推进不是只扫描新编号，也不重复执行已有有效回执的动作。

先生成最小批量执行卡；集中确认后只执行卡内动作，单项受阻时继续其他独立对象。每轮内部必须复用本轮扫描和执行结果完成轻量结束核账，回读受影响对象、未闭环旧账和派生对象，核对四类差集并保存下一可靠截点；不要另建固定的“每日核账”步骤，也不要因此重新通读全部历史、附件或 Diff。只有任务中断、截点不可靠、数量矛盾、范围变化、怀疑遗漏或正式交接时，才升级为加强核账或全量推进。结束时输出本轮变化、四类差集、PR 堆叠树、Issue 依赖树、当前行动方、当前可执行项、外部门禁、使用者本人的必须/建议人工操作和下一可靠截点。
```

## 场景 4：汇报进展或最新拓扑

读取文档：`02-总指挥核心规则.md`

复制发送：

```text
请只做当前项目进展和拓扑汇报。
只刷新与当前目标有关的最小事实，不重复读取全部历史。
输出：目标版本、工作项关系、当前波次、已完成证据、真实阻断、下一责任人、下一步。
不要创建任务，不修改代码，不执行远端写操作。
```

## 场景 4A：汇总并批量反馈人工操作

读取文档：`03-专项任务卡模板.md`

场景 2A、3A、3B 已默认输出人工操作。只有需要重新汇总、补看或怀疑遗漏时，复制发送：

```text
请汇总当前轮次中所有需要【使用者本人】参与的事项，并生成一份可直接照做的批量人工操作清单。
先排除 AI 能自动完成的步骤；其他团队成员、领域专家或管理员的事项只列为外部门禁，不混入我的操作清单。
按优先级和依赖顺序排列；相同操作可以合并，但必须保留每个 Issue、PR 或任务的独立状态，并使用“对象类型+编号”避免同号混淆。
每组操作说明：关联对象、参与类型、是否阻断、推荐方案、最少步骤、目的、预期结果、异常时需要的截图/日志/现象，以及完成后可直接复制的批量反馈模板。
已能从项目、平台或状态索引读取的信息请自动预填，不要要求我查找技术 ID 或重复填写环境信息。
```

完成一批人工操作后，复制发送：

```text
以下是本轮人工操作反馈，请读取并更新每个对象的独立状态；单项失败、未执行或证据不足不得被推定为通过，也不得暂停其他独立对象。

证据附件根目录：【无 / 本地绝对路径】(Windows 示例：C:\path\to\evidence；macOS/Linux 示例：/path/to/evidence)
共同说明：【无 / AI 无法从附件和项目中判断的补充】(示例：所有截图均来自同一次测试)

【Issue / PR / 任务 + 编号】
- 结果：【通过 / 失败 / 部分通过 / 未执行】
- 实际观察：【无补充 / 观察到的现象】(示例：点击后页面无响应)
- 证据位置：【无 / 对应子目录或文件名】(示例：issue-1/error.png)
- 额外说明：【无 / AI 无法自动判断的信息】(示例：本次使用移动网络测试)

请自动核对附件与编号的映射、判断证据是否足够、更新阻断和恢复条件，并继续所有能够自动推进的事项。只有确实还需要我参与时，才重新生成最小人工操作包和可复制提示词。
```

对象很多时可以把重复块改成紧凑表格：`对象｜结果｜实际观察｜证据位置｜额外说明`。AI 应预填对象列表，使用者本人只补观察结果；没有反馈的对象保持“未反馈”，不得按通过处理。

附件目录建议：每一批人工操作建立一个独立根目录，例如 `人工反馈/<YYYYMMDD>-本轮/`，再按 `issue-<NUMBER>`、`pr-<NUMBER>`、`task-<TASK_ID>` 建立子目录；多个对象共用的证据放在 `shared`，并在反馈中写明覆盖哪些对象。文件可用 `01-before.png`、`02-after.png`、`error.png`、`notes.txt` 等简短名称。只需把本批次根目录的绝对路径发给 AI，不必逐个上传；AI 应自动递归读取和映射。

不要只用数字命名子目录，否则 Issue 和 PR 同号时会冲突。含个人数据、受监管数据、账号、密钥或其他敏感信息的附件应先脱敏，默认放在仓库外；确需放入项目目录时只能使用项目已忽略的调试目录，并确认不会被 Git 提交。附件只能作为证据，不能因为文件存在就自动判定测试通过。

## 场景 4B：在新任务中解释上一轮某个对象

总指挥提醒适合分流时，打开一个新任务并复制下面提示词；也可以明确要求当前 AI 创建新任务。这样做的目的是让原总指挥继续推进主线，同时让新任务只读取目标对象的短回执和当前事实。

```text
请从项目总指挥入口恢复与【对象类型 + 编号】有关的最近可靠执行回执和 GitHub 当前事实，只读解释：上一轮实际做了什么、为什么这样做、哪些替代动作没有采用及原因、原结论是否因新事实失效、当前行动方和下一步。
只读取该对象的决策回执、证据指针和验证结论；不要重读无关聊天、附件、历史或 Diff。不要修改代码、中央状态或远端平台。
如果找不到完整的可迁移决策回执，明确告诉我回到原总指挥补录，不要根据零散历史猜测。
```

预期结果：新任务能独立解释该对象，又不接管主线。若它报告回执缺失、事实冲突或截点失效，把报告发回原总指挥补录或重新核验；不要让两个任务同时写中央状态或执行同一对象。

## 场景 5：项目状态或阶段目标发生变化

读取文档：`04-状态、目标变更与交接规范.md`

复制发送：

```text
项目发生了以下变化：
变化内容：【AI 无法从项目或平台自动发现的变化】
非平台来源：【无 / 会议、私聊或人工决定的可核验摘要】(示例：会议决定暂停旧方案并采用新接口)
请判断它影响哪些目标、Issue/PR、专项任务和验证结论。
暂停或标记失效的任务，保留原因和证据；只重算受影响子图。
不要删除历史，不要自动吸收未确认的新目标，不执行远端写操作。
最后汇报：继续项、暂停项、被替代项、需重测项和需要我确认的决定。
```

## 场景 6：交接给下一任总指挥

读取文档：`04-状态、目标变更与交接规范.md`、`07-总指挥交接记录模板.md`

复制发送：

```text
请准备总指挥交接。
先只读检查当前是否存在未核验的高风险动作、未记录的任务断点、重复调度或事实冲突。
先按规则健康检查并在需要时压缩当前进度视图；不得删除原始证据或让历史失去回查入口。
生成一份便携交接快照：事实截点、当前目标、代码与远端基线、活跃任务、阻断、未提交成果、下一责任人、验证证据入口、禁止项、恢复条件、失效条件和第一次只读动作。
未通过稳定截点检查前，不停止旧总指挥，不启动新总指挥调度。
```

如果只是临时交给其他 AI 执行一段任务、不转移总指挥权限，把生成的便携快照连同下面提示词发给它：

```text
请把附件视为有失效条件的只读交接快照，先核对项目规则、当前代码和远端事实，再处理我明确交给你的范围。
当前总指挥仍是中央状态的唯一写入者；不要直接覆盖中央进度、拓扑或状态索引。
你可以更新收到的便携交接快照副本。结束时返回：更新后的快照、实际完成、代码/远端变化与回执、验证、未完成项、阻断、下一责任人，以及可由总指挥核验写回的状态增量。
任何远端写操作仍须遵守当前项目规则和本轮精确授权。
```

## 场景 7：复盘最近一批任务

读取文档：`06-复盘与优化规则.md`

复制发送：

```text
请复盘最近一批任务，只基于实际工具调用、任务回执、验证结果和人工介入记录。
指出哪些步骤消耗了不必要的 Token、哪些任务拆分或模型选择不合适、哪里产生重复读取或重复汇报。
给出最多 3 条可执行改进，并说明预计减少的读取、轮次或人工确认。
不要因为复盘而修改项目代码或远端状态。
```

## 场景 8：暂停本轮工作

读取文档：`04-状态、目标变更与交接规范.md`

复制发送：

```text
请安全暂停当前 Goal。
先保存当前范围快照、任务断点、未提交修改、验证结果、阻断和恢复条件。
不要删除任务、分支、worktree 或历史记录；不要执行远端写操作。
最后给出下一次恢复时应先读取的文档和第一步只读检查。
```

## 远端协作授权：默认由 AI 生成

```text
请根据只读扫描结果自动生成一张最小批量执行卡，自动填写卡 ID、精确对象、当前 Base/Head、允许动作、分支、验证、禁止项、有效期、失效条件和失败回退。
把最终确认文本一并生成给我复制；我只负责确认、拒绝，或补充 AI 无法决定的限制。
```

确认文本可直接写“我确认刚刚生成的批量执行卡，按卡内边界执行”。只有同时存在多张待确认卡时，AI 才在文本中自动带入对应卡 ID；不得要求使用者本人手工查找。对象必须扫描后才能确定时，不填写猜测编号。

批量执行卡的正文必须让非专业使用者也能判断，固定先写：AI 推荐、为什么、最坏可能结果、仓库硬保护、授权后会自动做什么、明确不会做什么、什么情况下自动跳过。编号、Base/Head、分支和精确 Review 内容放在附录，不用它们替代人话风险说明。

只有人类、项目规则或平台真实授予的权限才有效。AI 的自动预填和历史授权记录不能替代本轮最终确认。
